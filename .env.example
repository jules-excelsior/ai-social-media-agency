PORT=3000
ADMIN_PASSWORD=admin2025

# DeepSeek API key — set this so users can generate without entering their own key.
# Stored in .env (gitignored) for local dev, or Vercel Environment Variables for production.
DEEPSEEK_API_KEY=

# Optional: set a default model (deepseek-chat or deepseek-reasoner)
# DEFAULT_MODEL=deepseek-chat
